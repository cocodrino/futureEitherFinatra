import sbt._

object MyBuild extends Build {
   /*lazy val root = Project("root", file(".")) dependsOn(postgresql)
   lazy val postgresql =
      RootProject(uri("git://github.com/finagle/finagle-postgres.git"))
      */
}
